# Stage 1 backlog: serial self-build.
# Format: <short-name> | <description>
#
# SPEC.md in this repo is the authoritative specification. These descriptions
# are a compressed index of it, not a replacement — when dispatching a task,
# the agent should read the relevant SPEC.md section too. Where a description
# here and SPEC.md disagree, SPEC.md wins; say so rather than silently picking.
#
# `./yolotown run [tasks.txt]` now reads this file and dispatches every task
# in it serially. Note that a bare `./yolotown run` defaults to THIS file —
# use a scratch tasks file when experimenting, or you will dispatch the whole
# remaining backlog for real.
#
# Stage 1 discipline still applies: dispatch one task at a time, review the
# green branch, merge, then start the next —
#   ./yolotown run-one <short-name> "<description>"
# The human is the conflict detector until Stage 3's conflict detection lands.
#
# DONE: config-loader (ebb6522), tasks-parser (fd212c3), run-dir (2a913ec),
#       rundir-per-task (43d0f80), report (504743d), cli (7c8d32b),
#       task-core (e767c40), agent-stdin (a2ee297),
#       agent-precheck (240c4f1), fan-out (8caca97),
#       conflict-detect (04a524a), plan-cmd (34f09d1).
#
# STAGE 1 COMPLETE. Next is Stage 3: conflict detection on PLANNER_MODEL with
# the plan.json contract, the real `plan` subcommand, the gated refactor stage
# with stdin approval, lane-violation warnings, and sequential execution of the
# INHERENTLY-COUPLED bucket. See SPEC.md sections 3.1, 3.2 and 6.


refactor-gate | Implement the gated refactor stage per SPEC.md section 3.2 — the one human checkpoint in the whole pipeline, so it must be conservative. For each COLLIDING-SPLITTABLE group in plan.json, generate a refactor PLAN ONLY via one headless call on PLANNER_MODEL: which files split, into what, what moves where, and why that makes the colliding tasks disjoint. Print it and STOP for stdin approval — approve/reject only; `edit` is deferred (SPEC.md section 9). Default to reject on empty input or EOF, exactly as the base-branch confirmation in seed.sh does. Only on approval execute it: a headless agent on PLANNER_MODEL performs the refactor in its own worktree cut from BASE_BRANCH, behavior-preserving changes only, then the full suite runs there. On green, BASE_BRANCH advances by exactly that refactor commit (fast-forward; the run holds the base so it cannot have moved) — this is the ONE place the tool ever writes to the base branch, permitted by SPEC.md section 3.2 precisely because a human approved it and the suite is green. On red: discard the worktree, leave BASE_BRANCH untouched, report. Never weaken or edit an existing test to make it pass. Never fan out on a red baseline. The plan-generation and refactor-execution invocations must each be distinguishable in tests/fake-claude (marker in prompt, own FAKE_CLAUDE_*_MODE), as the probe and planner already are — no existing test may change. Ship with tests covering approve, reject, EOF-defaults-to-reject, the red-suite path proving BASE_BRANCH is byte-identical afterwards, and the green path proving BASE_BRANCH advanced by exactly one commit.
lane-check | Implement lane-violation warnings per SPEC.md section 2. At gate time, diff the files the agent actually touched against the files plan.json predicted for that task. Straying outside the predicted lane writes a warnings/<task> record — the seam lib/report.sh already reads to render PASSED-WITH-WARNING — and must NEVER fail the task; this is warn-only, and making it blocking is an explicitly deferred decision (SPEC.md section 7). A task with no plan.json prediction (a run-one, say) is not a violation and must not warn. Ship with tests covering an in-lane task, a straying task, and a task with no prediction.
coupled-serial | Make the INHERENTLY-COUPLED bucket run sequentially, each task rebased on the previous one's result, per SPEC.md section 3.1. This is the one place tasks must NOT be parallel: they touch the same logic by definition. Extend the fan-out scheduler so a coupled group is dispatched in order, with each task cut from the previous task's branch rather than from BASE_BRANCH, and abandon the rest of a group when one of its tasks fails — a task rebased on a failure is meaningless. Tasks left undispatched that way are SKIPPED, which is exactly what that state exists for. Ship with tests proving the ordering, the rebasing, and that a mid-group failure skips the remainder rather than running it.
run-wiring | Wire the Stage 3 pipeline into `run` so it executes SPEC.md section 3 end to end: (1) conflict detection via lib/plan.sh writes plan.json into the run dir; (2) the refactor gate runs for every COLLIDING-SPLITTABLE group and stops for approval — a rejection or a red refactor aborts the run before any task worktree is cut, never fanning out on a red baseline; (3) DISJOINT tasks fan out in parallel via lib/fanout.sh, with lane checks at their gates; (4) INHERENTLY-COUPLED groups run via the coupled-serial scheduler; (5) the fan-in report. Add a --no-plan flag that skips steps 1 and 2 and treats every task as DISJOINT — the Stage 2 behavior, for a human who wrote the backlog to be disjoint and does not want to pay for a planner call — and print a loud one-line notice in the run header when it is used. run-one is unchanged: a single task has nothing to collide with. Ship with tests proving the ordering (plan before refactor before fan-out), that a rejected refactor aborts before any task worktree exists, and that --no-plan never invokes the planner.
